/* =====================================================================
   Firebase config — paste your project's Web App config below to turn on
   login + instant multi-device sync. Leave the PASTE_… placeholders and
   the app stays fully offline / local (no accounts, no network needed).

   This file is SAFE to commit: a Firebase *web* config is public by design
   — your data is protected by the Database Rules (see firebase-rules.json).
   Step-by-step setup is in SETUP.md.
   ===================================================================== */
var firebaseConfig = {
  apiKey: "PASTE_API_KEY",
  authDomain: "PASTE_PROJECT.firebaseapp.com",
  databaseURL: "https://PASTE_PROJECT-default-rtdb.firebaseio.com",
  projectId: "PASTE_PROJECT",
  appId: "PASTE_APP_ID"
};
